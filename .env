# Force UTF-8 mode for Python on all platforms (required on Windows to avoid
# UnicodeEncodeError when scripts print emoji to the console).
# uv automatically loads this file when running scripts via `uv run`.
PYTHONUTF8=1
